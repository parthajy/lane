The update manifest Lane polls is served from here, at
`https://lane.so/updates/darwin-aarch64.json`. `scripts/publish-update.sh`
writes it; commit it with each release. The artefact it points at lives on the
repository's releases page.
