{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window: core IPC and events, plus the updater (signed manifest from lane.so) and relaunch. No shell or filesystem plugins.",
  "windows": [
    "main",
    "overlay",
    "notch"
  ],
  "permissions": [
    "core:default",
    "updater:default",
    "process:default",
    "notification:default"
  ]
}
